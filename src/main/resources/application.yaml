spring:
  data:
    mongodb:
      database: ${rp.mongodb.database}
      uri: ${rp.mongodb.uri}

rp:
  pool:
    corePoolSize: 6
    maxPoolSize: 8
  mongodb:
    uri: mongodb://192.168.100.2:27017
    database: reportportal

  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driverClassName: org.postgresql.Driver
    jdbcUrl: ${rp.db.url}
    username: ${rp.db.user}
    password: ${rp.db.pass}
    maximumPoolSize: 14

  db:
    url: jdbc:postgresql://${rp.db.host}:${rp.db.port}/${rp.db.name}
    name: reportportal
    host: localhost
    port: 5432
    user: rpuser
    pass: ivanbudaev4ushka

  binarystore:
    path: /Users/pavel_bortnik/ReportPortal5/data/storage
    conatiner:
      path: /data/storage
    minio:
      endpoint: http://localhost:9000
      accessKey: minio
      secretKey: minio123
    # could be one of [seaweed, filesystem, minio]
    type: minio

  ##keep launches from date in ISO_LOCAL_DATE (yyyy-MM-dd)
  launch:
    keepFrom: 2019-03-30
  test:
    keepFrom: 2019-03-30
  log:
    keepFrom: 2019-03-30
  attach:
    keepFrom: 2019-03-30
  user:
    keepFrom: 2019-03-30


logging:
  level:
    root: info
  pattern:
    console: '%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %logger.%M - %msg%n'

